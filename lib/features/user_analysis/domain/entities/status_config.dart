import 'dart:ui';

class StatusConfig {
  final Color bg;
  final Color text;
  final String label;
  const StatusConfig({
    required this.bg,
    required this.text,
    required this.label,
  });
}