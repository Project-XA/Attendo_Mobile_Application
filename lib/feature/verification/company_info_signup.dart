 import 'package:flutter/material.dart';

class CompanyInfoSignup extends StatelessWidget {
  const CompanyInfoSignup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}